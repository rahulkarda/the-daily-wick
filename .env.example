# Required for daily generation pipeline
GEMINI_API_KEY=

# Required for hero-image fetching
UNSPLASH_ACCESS_KEY=

# Required for newsletter sending
BUTTONDOWN_API_KEY=

# Site URL — used in email absolute URLs and OG tags
SITE_URL=http://localhost:4321

# Curator name shown in bylines and AI-disclosure footer
# (Find-replace 'the Editor' across the codebase if you want a real name)
CURATOR_NAME=the Editor
